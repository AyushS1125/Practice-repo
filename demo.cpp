#include <iostream>  // Corrected #include

using namespace std;  // This allows us to use 'cout' directly

int main() {
    cout << "hello";  // No need to write 'std::cout' because we are using 'namespace std'
    return 0;
}
